version: "3.8"

services:
  redis:
    image: redis:7-alpine
    container_name: rss-telegram-bot-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes${REDIS_PASSWORD:+" --requirepass ${REDIS_PASSWORD}"}${REDIS_USER:+" --user ${REDIS_USER}"}
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  bot:
    build: .
    container_name: rss-telegram-bot
    restart: unless-stopped
    env_file: .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=${REDIS_PORT:-6379}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - REDIS_USER=${REDIS_USER:-}
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - bot-config:/app/config

volumes:
  redis-data:
    driver: local
  bot-config:
    driver: local
